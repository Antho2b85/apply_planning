<nav class="navbar navbar-expand-lg bg-corsica-red navbar-corsica">
  <div class="container-fluid">
    <span class="navbar-brand h1">Bienvenue sur Corsica Linea planning</span>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">

        <li class="nav-item">
          <a class="nav-link {{ app.request.attributes.get('_route') == 'app_admin_planning' ? 'active' : '' }}"
             aria-current="page"
             href="{{ path('app_admin_planning') }}">
            Planning général
          </a>
        </li>

        <li class="nav-item">
          {% if is_granted('ROLE_ADMIN') %}
          <a class="nav-link" data-bs-toggle="modal" data-bs-target="#modalAjouterAgent" href="#">Ajouter agent</a>
          {% endif %}
        </li>

        <li class="nav-item">
          {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_CHEF') %}
            {% if app.request.attributes.get('_route') == 'app_admin_planning' %}
              <a class="nav-link" href="#" id="btnExporterPDF">Exporter PDF</a>
            {% endif %}
          {% endif %}
        </li>

        <li class="nav-item">
          <a class="nav-link {{ app.request.attributes.get('_route') == 'app_arrivees_du_jour' ? 'active' : '' }}"
             href="{{ path('app_arrivees_du_jour') }}">
            Arrivée du jour
          </a>
        </li>

        <li class="nav-item">
          <a class="nav-link {{ app.request.attributes.get('_route') == 'app_previsions_du_jour' ? 'active' : '' }}" href="{{ path('app_previsions_du_jour') }}">Prévisions du jour</a>
        </li>

        <li class="nav-item">
          <a class="btn btn-perso" href="{{ path('app_logout') }}">Déconnexion</a>
        </li>

      </ul>
    </div>
  </div>
</nav>
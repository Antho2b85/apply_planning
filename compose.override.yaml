services:
  ###> doctrine/doctrine-bundle ###
  # J'ai supprimé le service PostgreSQL car j'utilise MySQL et MongoDB désormais.
  ###< doctrine/doctrine-bundle ###

  ###> symfony/mailer ###
  mailer:
    image: axllent/mailpit
    ports:
      - "1025"
      - "8025:8025" # Modifié ici pour voir l'interface web de Mailpit sur http://localhost:8025
    environment:
      MP_SMTP_AUTH_ACCEPT_ANY: 1
      MP_SMTP_AUTH_ALLOW_INSECURE: 1
###< symfony/mailer ###
